import { MovieList } from '../../components/MovieList';
import styles from './styles.module.scss';

export function Home(){
   return (
      <main>
         <MovieList></MovieList>
      </main>
   )
}